# Fine-tuning stack for gpt-oss-20b with Unsloth (4-bit LoRA).
# Requires: Linux or WSL2, NVIDIA GPU with CUDA 12.x, Python 3.10-3.12.
# gpt-oss needs torch >= 2.8 and triton >= 3.4.
torch>=2.8.0
torchvision
triton>=3.4.0; platform_system == "Linux"
# Triton MXFP4 kernels used by gpt-oss (not published to PyPI, Linux only).
# Not listed here: installing it requires git-cloning the full triton repo,
# which is huge and flaky enough to kill the worker bootstrap. It is installed
# with retries in the "Install dependencies" step of orchestra.yml instead.

unsloth[base]
unsloth_zoo[base]
transformers>=4.55.0
trl
peft
accelerate
bitsandbytes

datasets
pandas
pyarrow
matplotlib

# Downloads the Kaggle dataset automatically when not running on Kaggle
kagglehub
